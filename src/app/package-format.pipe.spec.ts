import { PackageFormatPipe } from './package-format.pipe';

describe('PackageFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PackageFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
